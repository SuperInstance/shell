[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "bootstrap"
version = "0.1.0"
description = "shell — extracted from SuperInstance oracle1-workspace"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
